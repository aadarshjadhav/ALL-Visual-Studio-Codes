import { PowerBasePipe } from './power-base.pipe';

describe('PowerBasePipe', () => {
  it('create an instance', () => {
    const pipe = new PowerBasePipe();
    expect(pipe).toBeTruthy();
  });
});
